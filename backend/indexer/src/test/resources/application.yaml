server:
  port: 0
spring:
  kafka:
    consumer:
      auto-offset-reset: latest
neo4j:
  url: url
  username: username
  password: password
kafka:
  url: url
  in:
    topic: topic
    groupId: groupId
  out:
    topic: topic
domain:
  supportedLocales: en
logging:
  level:
    com.osearch.indexer: DEBUG
management:
  endpoint:
    health:
      probes:
        enabled: true
